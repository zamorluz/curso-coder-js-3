const __report = (message, label, category = 'general' ) => {
    // console.table({
    //     category,
    //     label,
    //     message,
    //     browser: getBrowser(),
    //     timestamp: getFullDate() 
    // });
};